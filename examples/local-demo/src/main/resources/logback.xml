<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender for container stdout -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- JSON appender for structured logging (optional, for log aggregation) -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="ch.qos.logback.contrib.json.classic.JsonLayout">
                <jsonFormatter class="ch.qos.logback.contrib.jackson.JacksonJsonFormatter">
                    <prettyPrint>false</prettyPrint>
                </jsonFormatter>
                <timestampFormat>yyyy-MM-dd'T'HH:mm:ss.SSS'Z'</timestampFormat>
                <timestampFormatTimezoneId>UTC</timestampFormatTimezoneId>
                <appendLineSeparator>true</appendLineSeparator>
            </layout>
        </encoder>
    </appender>

    <!-- Delos loggers -->
    <logger name="com.hellblazer.delos" level="INFO"/>
    <logger name="com.hellblazer.delos.demo" level="INFO"/>
    <logger name="com.hellblazer.delos.fireflies" level="INFO"/>
    <logger name="com.hellblazer.delos.choam" level="INFO"/>
    <logger name="com.hellblazer.delos.stereotomy" level="WARN"/>

    <!-- gRPC logging -->
    <logger name="io.grpc" level="WARN"/>
    <logger name="io.netty" level="WARN"/>

    <!-- Metrics logging -->
    <logger name="io.prometheus" level="WARN"/>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
